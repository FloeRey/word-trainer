import { Config } from '../app/types/config';

export const config: Config = {
  languages: {
    de: true,
    en: false,
    fr: false,
    it: false,
  },

  naming: {
    de: 'deutsch',
    en: 'englisch',
    fr: 'französisch',
    it: 'italienisch',
  },
};
